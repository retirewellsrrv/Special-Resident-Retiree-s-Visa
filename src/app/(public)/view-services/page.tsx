import { getServicePlans } from "@/actions/admin/service"

export default async function Services() {
    //! commented for now it has an error needs to be fix
    // const services = await getServicePlans() 
    return (
        <h1>
            Services
        </h1>
    )
}